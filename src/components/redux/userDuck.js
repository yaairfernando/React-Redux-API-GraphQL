// Constants
let INITIALDATA = {
  loggedIn: false
}
const LOGIN = "LOGIN";

// Reducer
export default function reducer(state = INITIALDATA, action){
  switch(action.type){
    case LOGIN:
      
    default:
      return state;
  }
}

// Action Creators